apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  values:
    crds:
      enabled: true
    alertmanager:
      enabled: false
    grafana:
      enabled: false
